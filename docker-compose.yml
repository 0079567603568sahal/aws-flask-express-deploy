version: '3.8'
services:
  backend:
    build: ./Backend
    ports:
      - "5000:5000"

  frontend:
    build: ./Frontend
    ports:
      - "3000:3000"
    environment:
      - BACKEND_URL=http://localhost:5000
